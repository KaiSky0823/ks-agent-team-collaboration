---
name: ks-agent-team-collaboration
description: >-
  多 agent / 多团队 / 前后端协作的架构与协作纪律，专治"一端改了契约、另一端不知道"这个多人协作最高频 bug 源（实测占某轮 e2e bug 的 47%）。
  当规划或编排多个并行 agent 工作流、设计前后端/模块间的接口契约、决定谁拥有权威状态、或搭建有多个协作者/agent 的项目时使用。
  核心原则：单一权威状态源（一端算、其他端只派生不本地猜不本地缓存）、契约文档化（改契约先改文档再改代码）、
  工具强制标注影响面（frontend-impact: yes/no）、共享数据层、跨领域改动先打招呼、变更可追溯。
  触发词：多 agent 编排、并行 workstream、前后端契约、接口契约、谁拥有这个状态、single source of truth、
  多人协作、契约、单一权威源、frontend-impact、跨团队。
---

# Agent Team 协作与架构纪律

> 来自资深 CTO 实践，适用于多 agent / 多人 / 前后端协作。
> **直接掐死多人协作最高频的 bug 源："一端改了契约，另一端不知道"。**
> 实测：某轮端到端测试 **47% 的 bug** 都是前后端契约断裂——下面这套机制命中根因后，同类 bug 修复速度快数倍。

来源：从多 agent 协作项目实践蒸馏（GENERIC_AGENT_TEAM_PM_PRINCIPLES）。配套审查纪律见 `ks-agent-team-review`。

---

## 原则 1：前后端纠验机制（最高价值）
- **单一权威状态源**：一端（通常后端）计算权威状态，另一端（前端）**只派生、不本地猜测、不本地缓存**。
- **契约文档化**：接口契约写成正式文档；**改契约必须先改文档，再改代码**。
- **工具强制**：pre-commit hook 强制每个 commit 标注是否影响对端（如 `frontend-impact: yes/no`）。

## 原则 2：共享数据层纪律
- 开发 / 测试 / 生产连**同一共享数据源**，不自建本地副本。
- 避免"本地能跑、生产挂"的环境不一致掩盖问题。

## 原则 3：跨领域改动事先沟通
- 碰别人领域（后端 / 数据库 / 架构）前**先打招呼**，不闷头改。
- 各人守自己领域边界；不确定归属先问，不自行越界。

## 原则 4：部署 / 同步纪律
- 改动 **push 到共享处后才通知**协作方（否则对方看不到）。
- 部署方式团队约定一致，不混用。
- **线上先保持已验证的稳定版**；新代码先本地 / 预发验证通过，再推线上。

## 原则 5：变更可追溯
- commit message 覆盖**完整范围**；一个 commit 含多批改动时全部标注，不漏。

## 原则 6：按宿主能力派发 agent
- 只并行处理可独立、边界明确的子任务；顺序依赖留在主线。
- Claude Code 使用当前可用的 Agent／Workflow；Codex 使用当前可用的 collaboration。默认继承会话模型与推理强度；只有用户或适用项目规则明确要求时才覆盖。
- 按任务复杂度和宿主实际可用模型配置，不跨宿主照搬模型别名、fork 或 effort 参数；未验证的能力不承诺。
- 给子任务最少必要上下文，按可用并发槽分波执行；共享配置与最终集成由一个 writer 负责。

---

## 怎么在"派多个 agent 并行"时落地（高频场景）

当你用 `collaboration.spawn_agent` 同时派多个 build agent，这些原则翻译成具体编排纪律：

1. **先定契约再 fan out**：并行 agent 动手前，先把它们之间的接口契约定死（数据模型、API 形状、共享模块签名），写进 prompt 当硬约束——这就是"契约文档化 + 单一权威源"。
2. **分配不相交的文件/目录**：每个 agent 只写自己的目录，**禁止改共享文件**（如主路由注册、依赖清单、数据模型）——共享文件由编排者（你）统一改。这避免并发写冲突 = 物理版的"守边界"。
3. **影响面显式上报**：要求每个 agent 在总结里写明"我新增的对外接口/我需要的依赖/给集成者的注意点"——等价于 `frontend-impact` 标注。
4. **交界处重点查**：多 agent 的产出在**模块交界 / 前后端契约**处最易断（见 `ks-agent-team-review` 铁律 6）——集成时两端都查对齐。
5. **共享同一数据/地基**：所有 agent 建在同一份已验证的地基（同一数据模型、同一 DB schema、同一客户端封装）上，不各搞一套。

> 一句话：**单一权威源 + 契约文档化 + 强制标注影响面 + 守边界**——在 fan out 前把契约钉死，在集成时把交界查穿。
